<h1>req.path</h1>
<p>リクエストの<a href="http://nodejs.org/api/http.html#http_message_url">リクエストURL文字列</a>から得たURLパス名です。これはURLのスラッシュ以降(例: <code>/foo/bar</code>)の部分を示しますが、クエリ文字列(例: <code>?name=foo</code>)やフラグメント(例:<code>#foobar</code>)を示さないことにご留意下さい.</p>
<h3>使い方</h3>
<pre><code class="language-js">req.path;</code></pre>
<h3>例</h3>
<p>クライアントが以下のリクエストを送ったとします。:</p>
<blockquote>
<p><a href="http://localhost:1337/donor/37?name=foo#foobar">http://localhost:1337/donor/37?name=foo#foobar</a></p>
</blockquote>
<p><code>req.path</code> は以下のように定義されます:</p>
<pre><code class="language-js">req.path;
// -&gt; "/donor/37"</code></pre>
<docmeta name="uniqueID" value="reqpath216836">
<docmeta name="displayName" value="req.path">