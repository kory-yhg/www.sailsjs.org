<h1>sails.sockets.socketRooms( <code>socket</code> )</h1>
<h3>Purpose</h3>
<p>Get the list of rooms a socket is subscribed to</p>
<h3>Overview</h3>
<h4>Parameters</h4>
<table>
<thead>
<tr>
<th></th>
<th>Description</th>
<th>Accepted Data Types</th>
<th>Required ?</th>
</tr>
</thead>
<tbody>
<tr>
<td>1</td>
<td>Socket</td>
<td><code>object</code></td>
<td>Yes</td>
</tr>
</tbody>
</table>
<h3>Example Usage</h3>
<pre><code class="language-javascript">// Controller action

getMyRooms: function(req, res) {
    var roomNames = JSON.stringify(sails.sockets.socketRooms(req.socket));
    res.json({
      message: 'I am subscribed to: '+roomNames
    });
}</code></pre>
<h3>Notes</h3>
<blockquote>
<ul>
<li>The phrase &quot;request socket&quot; here refers to an application-layer WebSocket/Socket.io connection.  <code>req.socket</code> also exists for HTTP requests, but it refers to the underlying TCP socket at the transport layer, which is different.  Be sure and ensure <code>req.isSocket == true</code> before using <code>req.socket</code> with this method.</li>
</ul>
</blockquote>
<docmeta name="uniqueID" value="sailssocketssocketRooms270469">
<docmeta name="displayName" value="sails.sockets.socketRooms()">