<h1>myApp/api/controllers/.gitkeep</h1>
<h3>目的</h3>
<p>このファイルは無視してください。これが存在する理由は<code>git</code>が空のディレクトリをリモートフォルダに転送するのを拒否するからです。<code>.gitkeep</code>は空のディレクトリを区別しないようにした人たちによる非正規の慣習です。</p>
<docmeta name="uniqueID" value="gitkeep271990">
<docmeta name="displayName" value=".gitkeep">