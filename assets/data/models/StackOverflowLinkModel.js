/**
 * Stack overflow link model definition
 */

Mast.modelDefinitions.StackOverflowLinkModel = Backbone.Model.extend({
    defaults: {
        url: 'http://stackoverflow.com',
        page: 'something'
    }
});
